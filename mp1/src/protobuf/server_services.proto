syntax = "proto3";
package serverservices;

service ServerServices {
    rpc ReturnMatches (Cmd) returns (stream GrepLine) {
    }
}

message GrepLine {
    string line = 1;
}

message Cmd {
    string cmd = 1;
}